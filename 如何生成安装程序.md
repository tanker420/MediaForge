# 如何拿到 MediaForge 安装程序（小白版）

全程在**网页上点鼠标**，不用装任何软件，不用敲命令。
大约需要 **10 分钟**（其中 8 分钟是电脑自动干活，你只需等待）。

---

## 开始之前

你需要：一个能上网的浏览器，和你的 GitHub 账号（就是存放这份代码的账号）。

**为什么要这一步？** 因为我（AI）运行在 Linux 系统上，没法直接生产 Windows 的 `.exe` 文件。
所以我们要借助 GitHub 提供的免费 Windows 电脑，让它帮我们生成安装包。这是免费的。

---

## 第 1 步：打开你的仓库网页

在浏览器地址栏输入下面这个网址，回车：

```
https://github.com/tanker420/-
```

看到代码文件列表，就说明打开对了。

---

## 第 2 步：新建一个文件（关键的一步）

我们要告诉 GitHub："请用 Windows 电脑帮我打包"。方法是新建一个配置文件。

### 2.1 点击新建文件按钮

在页面上找到 **`Add file`** 按钮（在文件列表的右上方，绿色 `Code` 按钮旁边），
点它 → 在弹出的菜单里选 **`Create new file`**。

### 2.2 输入文件名（务必一字不差）

页面顶部会出现一个输入框，提示文字是 `Name your file...`。
在里面**完整粘贴**下面这一行：

```
.github/workflows/build-windows.yml
```

> 注意：最前面那个**点**不能少。当你输入斜杠 `/` 时，输入框会自动分成几段，
> 这是正常现象，说明 GitHub 识别出了文件夹，不用管它。

### 2.3 填入文件内容

现在需要把配置内容复制进去。**打开一个新的浏览器标签页**，访问：

```
https://github.com/tanker420/-/blob/arena/019fb701-repo/packaging/ci/build-windows.yml
```

在这个页面上：
- 找到右上角的**复制图标**（两个方块叠在一起的样子，鼠标悬停显示 `Copy raw file`）
- 点一下，内容就复制好了

然后**切回刚才新建文件的标签页**，在下方黑色的大编辑框里点一下，
按 **Ctrl+V** 粘贴。

### 2.4 保存

点击页面**右上角的绿色 `Commit changes...` 按钮**。
弹出一个小窗口，什么都不用改，直接点窗口里的绿色 **`Commit changes`** 按钮。

✅ 第 2 步完成。

---

## 第 3 步：让它开始干活

### 3.1 进入 Actions 页面

点击页面**最上方**的 **`Actions`** 标签（在 Code、Issues、Pull requests 这一排里）。

> 如果出现一个绿色按钮写着 `I understand my workflows, go ahead and enable them`，
> 点它一下。（首次使用需要确认，属正常流程）

### 3.2 启动构建

- 在**左侧栏**，点击 **`构建 Windows 安装程序`**
- 在右侧，找到蓝色小字 **`Run workflow`**，点它
- 弹出的小框里，直接点绿色的 **`Run workflow`** 按钮

### 3.3 等待

页面上会出现一行新记录，前面有个**黄色转圈**图标 🟡，表示正在打包。

**现在去泡杯茶，等 8-10 分钟。** 期间可以刷新页面查看进度。

转圈变成**绿色对勾** ✅ 就是成功了。

> 万一变成**红叉** ❌ 也别慌，往下看最后的"遇到问题"部分。

---

## 第 4 步：下载安装程序

1. 点击那条**绿色对勾**的记录，进入详情页
2. 滚动到页面**最下方**，找到 **`Artifacts`（产物）** 区域
3. 那里有两个文件，点击第一个即可下载：

| 名称 | 说明 |
|---|---|
| **MediaForge-Windows-Setup** | 👈 **下载这个**，标准安装程序 |
| MediaForge-Windows-Portable | 免安装版，解压就能用（备选） |

下载下来是一个 `.zip` 压缩包。**右键 → 全部解压缩**，
里面就是 **`MediaForge-1.0.0-Setup.exe`** —— 这就是你要的安装程序。

---

## 第 5 步：安装使用

双击 `MediaForge-1.0.0-Setup.exe`。

> **可能出现蓝色警告窗**："Windows 已保护你的电脑"。
> 这是因为程序没有购买数字签名证书（一年几千元），并非病毒。
> 点击窗口里的 **`更多信息`** → 再点 **`仍要运行`** 即可。

然后一路点"下一步"完成安装。安装时可以勾选：
- ✅ 创建桌面快捷方式
- ✅ 在右键菜单添加"用 MediaForge 转换"

装好后，桌面会出现图标，双击就能打开。
**FFmpeg 已经打包在里面了，你不需要再装任何东西。**

### 怎么用

1. 把视频/音频/图片文件**直接拖进**程序窗口
2. 右上角选择**输出格式**（比如 mp4、mp3、jpg）
3. 想省事就在**预设**下拉框里挑一个现成的（有 28 个）
4. 想精细调就在下面的标签页里改参数（画质、码率、分辨率等）
5. 点左上角 **`开始转换`**

---

## 遇到问题？

### ❌ 构建失败了（红叉）

点进那条红色记录，点击标着红叉的步骤名，会展开日志。
**把最后几行报错内容截图发给我**，我来修。

### 找不到 `Add file` 按钮

确认你登录的是**拥有这个仓库的账号**（tanker420）。
没登录的话，页面右上角会显示 `Sign in`，点它登录。

### Artifacts 区域是空的

说明构建还没跑完，或者失败了。回到 Actions 页面确认是否已变成绿色对勾。

### 想要新版本

以后每次代码更新，重复**第 3 步**（Actions → Run workflow）即可拿到新安装包，
不用再重复第 2 步。

---

## 另一条路：不想折腾网页？

如果你手头就有一台 Windows 电脑，也可以本地打包，但需要先装两个软件
（Python 和 Inno Setup），然后双击运行 `packaging\build_windows.bat`。
对小白来说，**推荐用上面的网页方式**，更省事。
