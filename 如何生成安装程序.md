# 如何生成 Windows 安装程序

MediaForge 的 Windows 安装包由 **PyInstaller**（打包成绿色目录程序）+ **Inno Setup**（打成中文安装程序）两步完成。

## 1. 环境准备

| 软件 | 版本 | 说明 |
| --- | --- | --- |
| Python | 3.10+ | 需要 64 位版本 |
| Inno Setup | 6.x | 官方下载：https://jrsoftware.org/isinfo.php |
| FFmpeg | 任意 | 可选；不装也能打包，但安装包内不含转换引擎 |

> 无需手动安装 FFmpeg：`packaging\ci\build.ps1` 会自动下载最新静态构建并放进 `bin\` 目录随程序分发。

## 2. 方式一：本地一键打包（Windows）

在仓库根目录双击运行：

```bat
packaging\build_windows.bat
```

脚本会依次完成：创建虚拟环境 → 安装依赖 → PyInstaller 打包（无控制台窗口）→ Inno Setup 生成中文安装包。

产物：

- `dist\MediaForge\MediaForge.exe` — 绿色版程序目录
- `dist_installer\MediaForge-<版本>-Setup.exe` — 中文安装程序

## 3. 方式二：GitHub Actions 自动构建

仓库已配置工作流 `.github/workflows/build-windows.yml`，一次触发会同时产出 **Windows 安装包 + Android APK**：

1. **手动触发**：仓库 Actions 页面 → 「构建 Windows 安装包与 Android APK」→ Run workflow（可选填版本号）
2. **自动触发**：推送 `v*` 标签（如 `v1.4.0`），自动构建并发布 Release

| Job | 运行环境 | 产物 |
| --- | --- | --- |
| `build-windows` | windows-latest（PowerShell 7） | `MediaForge-<版本>-Setup.exe` |
| `build-android` | ubuntu-latest（JDK 17 + Gradle 8.7 + Android SDK 34） | `app-release.apk` |
| `release` | 汇总两者，挂到 GitHub Release | — |

> 注：`packaging/ci/build-windows.yml` 是与真实工作流一致的参考模板，供复用到其它仓库。

### 3.1 安卓 APK 自动构建

工作流在 Ubuntu 运行器上自动：装 JDK 17 → 装 Android SDK（platform 34 + build-tools 34.0.0）→ 准备签名密钥 → `gradle :app:assembleRelease`。

**安卓签名（重要）**：为了与本地 `android/release.keystore`（alias/password 均为 `mediaforge`）的签名保持连续，建议把密钥配成仓库 Secret：

```bash
# 本地生成 base64（Windows PowerShell）
[Convert]::ToBase64String([IO.File]::ReadAllBytes("D:\Hermes\MediaForge\android\release.keystore"))
```

在仓库 **Settings → Secrets and variables → Actions** 添加：

| Secret 名 | 内容 |
| --- | --- |
| `ANDROID_KEYSTORE_BASE64` | 上述 base64 字符串（与本地密钥一致 → 可覆盖升级） |

未配置该 Secret 时，CI 会用 keytool 现场生成一次性密钥：APK 可以安装，但**与本地签名不一致**，覆盖升级会失败（需先卸载旧版）。

### 3.2 发版流程（git）

```bash
# ① 先把当前 main 备份到以版本命名的备份分支
git branch backup/v1.4.0

# ② 强制推送 main（确保工作流文件 .github/workflows/ 一定上传生效）
git push origin main --force

# ③ 建一个 1.4 版本的分支（release 分支）
git branch release/v1.4

# ④ 在 main 上打标签并推送 → 自动触发 CI 构建 + 发布 Release
git tag v1.4.0
git push origin main release/v1.4 backup/v1.4.0 v1.4.0
```

推送后 Actions 会自动完成 Windows 安装包 + Android APK 的构建，并把两者一起发布到 Releases 页。

## 4. 打包脚本说明

| 文件 | 作用 |
| --- | --- |
| `packaging\MediaForge.spec` | PyInstaller 配置；`console=False` 保证无命令行窗口 |
| `packaging\version_info.txt` | exe 文件属性（中文产品名、版本号） |
| `packaging\installer.iss` | Inno Setup 脚本；简体中文向导、桌面/开始菜单快捷方式、可选右键菜单、可选加入 PATH |
| `packaging\build_windows.bat` | 本地一键打包入口 |
| `packaging\ci\build.ps1` | CI/手动共用构建脚本（下载 FFmpeg → 打包 → 装包） |

## 5. 代码签名（可选，强烈建议）

未签名的 exe 在 Windows SmartScreen 与部分杀软下会被拦截或误报。生产环境请配置代码签名证书（OV / EV / Azure Trusted Signing 等），签名后的安装包会显著降低误报率。

### 5.1 本地签名

把 `.pfx` 证书放到本地任意目录，调用 `build.ps1` 时传入：

```powershell
powershell -ExecutionPolicy Bypass -File packaging\ci\build.ps1 `
  -Version 1.1.0 `
  -PfxPath D:\certs\mediaforge.pfx `
  -PfxPassword '你的密码'
```

`build.ps1` 会自动查找 Windows SDK 的 `signtool.exe`（默认在 `C:\Program Files (x86)\Windows Kits\10\bin\<ver>\x64\`），找不到时跳过签名但不报错。签名会作用于：
- `dist\MediaForge\MediaForge.exe`（主程序）
- `dist_installer\MediaForge-*-Setup.exe`（安装包）

时间戳服务器默认 `http://timestamp.digicert.com`（DigiCert），可改 `-TimestampUrl`。签名算法 SHA-256。

### 5.2 GitHub Actions 自动签名

在仓库 Secrets 添加：

| Secret 名 | 内容 |
| --- | --- |
| `CODE_SIGNING_PFX_BASE64` | base64 编码的 `.pfx` 文件内容（用 `base64 -i cert.pfx` 生成） |
| `CODE_SIGNING_PFX_PASSWORD` | `.pfx` 解锁密码 |

工作流会自动解码 PFX → 临时目录 → 传给 `build.ps1` → 签完删除临时文件。证书未配置时整个构建流程照常运行（仅不签名），便于测试。

### 5.3 注意事项

- **Windows SDK** 必须安装在构建机上（GitHub Actions 的 `windows-latest` 已自带）；
- **EV 证书** 通常带硬件 USB Key，需要在构建机上插着 Key 跑签名；
- **Azure Trusted Signing** 可走 REST API 签名，不需要本地 SDK——但 `signtool` 暂未原生支持，需要换用 AzureSignTool；
- 签名失败会抛错中断构建（避免静默发布未签名包）。

---

## 6. 常见问题

**Q：安装程序被杀毒软件误报？**
A：未签名程序在部分杀软下可能提示。申请代码签名证书（见第 5 节）签名后即可消除大部分误报。

**Q：安装后提示「未检测到 FFmpeg」？**
A：正常打包流程会把 FFmpeg 放进程序目录 `bin\`，不会出现此提示。若使用绿色版且缺少 `bin\`，可手动下载 ffmpeg.exe/ffprobe.exe 放入 `bin\`，再点击程序右上角 FFmpeg 状态标签重新检测。

**Q：为什么右键菜单默认不勾选？**
A：安装时「加入右键菜单」「加入 PATH」均为可选勾选项，保持默认即可获得更清爽的系统环境。

**Q：GitHub Actions 打包总是失败？**
A：常见原因：(1) `packaging\ci\build.ps1` 出现了中文字符——本项目已强制 ASCII；(2) BtbN 静态 FFmpeg 下载链接偶发 404——重新运行即可；(3) PyInstaller 7.x 改了 SPEC 注入语义——本项目已钉死在 `6.3~6.x`；(4) `packaging\installer.iss` 不是 UTF-8 BOM——本项目已修正。每次代码改动后请先用本地 `pyinstaller --noconfirm --clean packaging\MediaForge.spec` 验证。
