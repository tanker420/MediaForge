# 拿到安装程序：3 步，约 10 分钟

全程在浏览器点鼠标。不用装软件，不用敲命令，免费。
其中 8 分钟是电脑自动干活，你只要等。

---

# 第 1 步 · 新建一个文件

### 1-1　点这个链接

👉 **https://github.com/tanker420/-/new/main**

（如果提示登录，用你的 GitHub 账号 `tanker420` 登录）

### 1-2　填文件名

页面顶部有个输入框，写着 `Name your file...`。
把下面这行**完整复制**进去：

```
.github/workflows/build-windows.yml
```

> 最前面的**点**不能少。输入斜杠时框会自动分段，这是正常的。

### 1-3　填内容

在下面的大编辑框里点一下，把**下面整段**复制粘贴进去：

```yaml
name: 构建 Windows 安装程序

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: powershell -ExecutionPolicy Bypass -File packaging\ci\build.ps1
      - uses: actions/upload-artifact@v4
        with:
          name: MediaForge-Windows-Setup
          path: dist_installer/*
```

### 1-4　保存

点**右上角绿色的 `Commit changes...`** → 弹窗里再点绿色的 **`Commit changes`**。

✅ 第 1 步完成。

---

# 第 2 步 · 点一下"开始"

### 2-1　点这个链接

👉 **https://github.com/tanker420/-/actions/workflows/build-windows.yml**

> 若出现绿色按钮 `I understand my workflows, go ahead and enable them`，点一下它。

### 2-2　启动

- 右侧找到蓝色小字 **`Run workflow`**，点它
- 弹出的小框里，再点绿色的 **`Run workflow`**

### 2-3　等待

页面出现一行新记录，前面是**黄色转圈** 🟡 = 正在打包。

**去泡杯茶，等 8-10 分钟。** 刷新页面看进度。

变成**绿色对勾** ✅ 就是成功了。

✅ 第 2 步完成。

---

# 第 3 步 · 下载安装

### 3-1　下载

点那条**绿勾**记录 → 滚到页面**最底部** → 看到 **`Artifacts`** 区域 →
点 **`MediaForge-Windows-Setup`** 下载。

### 3-2　解压

下载的是 zip 压缩包。**右键 → 全部解压缩**，
里面的 **`MediaForge-1.0.0-Setup.exe`** 就是安装程序。

### 3-3　安装

双击它。

> ⚠️ 会弹**蓝色警告**"Windows 已保护你的电脑"。
> 这是因为没买数字签名证书（一年几千元），**不是病毒**。
> 点 **`更多信息`** → **`仍要运行`**。

之后一路点"下一步"就装好了。FFmpeg 已打包在内，**不需要再装任何东西**。

✅ 全部完成。

---

# 怎么用

双击桌面图标打开：

1. 把文件**拖进**窗口
2. 右上角选**输出格式**（mp4 / mp3 / jpg …）
3. 图省事就在**预设**里挑一个（有 28 个现成的）
4. 点左上角 **`开始转换`**

---

# 出问题了？

| 情况 | 怎么办 |
|---|---|
| 变成**红叉** ❌ | 点进去，点红叉那一步展开日志，**把报错截图发我** |
| 找不到 `Run workflow` | 说明第 1 步没成功，重做第 1 步 |
| Artifacts 是空的 | 还没跑完，等变成绿勾再看 |
| 以后想要新版本 | 只重复**第 2 步**即可，第 1 步不用再做 |
