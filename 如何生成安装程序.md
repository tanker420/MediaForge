# 拿到安装程序

**配置已就绪：版本号来自 git tag，构建与发布全自动。**

---

## 方式 A · 正式发版（推荐）

打一个 `v*` 标签并推送，GitHub Actions 会自动：

1. 从 tag 解析版本号（`v1.2.0` → `1.2.0`）
2. 下载 FFmpeg → PyInstaller 打包 → 冒烟测试
3. 用 Inno Setup 生成 `MediaForge-1.2.0-Setup.exe`
4. 生成 SHA256 校验文件
5. 上传 Actions Artifact（保留 14 天）
6. 创建 GitHub Release 并附上安装包

```bash
git tag v1.0.0
git push origin v1.0.0
# 去 Actions 看进度，完成后 Releases 页面自动出现安装包
```

发测试版（自动标为 Pre-release）：

```bash
git tag v1.1.0-beta.1
git push origin v1.1.0-beta.1
```

发错了要重来：

```bash
git tag -d v1.0.0
git push origin :refs/tags/v1.0.0
# 到 Releases 页面删掉对应 Release，再重新打 tag
```

👉 Actions：https://github.com/tanker420/MediaForge/actions/workflows/build-windows.yml  
👉 Releases：https://github.com/tanker420/MediaForge/releases

---

## 方式 B · 只验证构建（不发版）

手动触发 workflow：只构建 + 上传 Artifact，**不会**创建 Release。

1. 打开 👉 https://github.com/tanker420/MediaForge/actions/workflows/build-windows.yml
2. 点右侧 **`Run workflow`**
3. 分支选 **`main`**（或你要验证的分支）
4. 点绿色 **`Run workflow`**

### 等待

出现**黄色转圈** 🟡 = 正在打包。

**去泡杯茶，等 8–15 分钟**，刷新页面看进度。

变**绿色对勾** ✅ = 成功。

### 下载

1. 点那条**绿勾**记录
2. 滚到页面**最底部**，找到 **`Artifacts`**
3. 点 **`MediaForge-Windows-Setup-0.0.0-dev`**（手动触发时版本为 dev）下载
4. 下载的是 zip，**右键 → 全部解压缩**
5. 双击里面的 **`MediaForge-0.0.0-dev-Setup.exe`**

> ⚠️ 会弹**蓝色警告**「Windows 已保护你的电脑」。  
> 不是病毒，只是没买数字签名证书。  
> 点 **`更多信息`** → **`仍要运行`**。

一路「下一步」装完。FFmpeg 已打包在内，**不用再装别的东西**。

---

## ⚠️ 重要：每次都要发起「新运行」

**不要**点历史记录里的 `Re-run jobs`（重跑）——那会沿用旧代码，  
修复过的问题会重复出现。每次都从 `Run workflow` 按钮重新发起，  
或重新推送 tag。

---

## 怎么用

双击桌面图标：

1. 把文件**拖进**窗口
2. 右上角选**输出格式**（mp4 / mp3 / jpg …）
3. 图省事就在**预设**里挑一个（28 个现成的）
4. 点左上角 **`开始转换`**

---

## 版本号从哪来？

| 触发方式 | 版本号 | 是否发 Release |
|---|---|---|
| `git tag v1.2.0 && git push origin v1.2.0` | `1.2.0` | ✅ 是 |
| `git tag v1.2.0-beta.1 && push` | `1.2.0-beta.1`（Pre-release） | ✅ 是 |
| Actions 手动 `Run workflow` | `0.0.0-dev` | ❌ 否，仅 Artifact |

版本号**只有一个来源**：git tag。安装包文件名、Release 名称、程序内「关于」对话框全部自动派生，无需手动改任何文件。

---

## 还是失败？

点进红叉那一步，展开日志，**把报错截图发我**。
