# 拿到安装程序

**你已经建好文件了，现在只剩：点一下"运行" → 等 8 分钟 → 下载。**

---

## ⚠️ 上次失败的原因（已修好）

两个问题，都已修复，**你不用重做任何设置**：

1. 我的打包脚本里写了中文注释，Windows 老版本 PowerShell 读不懂中文，语法崩溃 → 已全改成英文
2. 你在 `main` 分支上运行，但代码在另一个分支上 → 下面给你正确的链接

---

# 第 1 步 · 点这个链接

👉 **https://github.com/tanker420/-/actions/workflows/build-windows.yml**

# 第 2 步 · 切换到正确的分支（重要！）

1. 点右侧蓝色小字 **`Run workflow`**
2. 弹出的小框里有个下拉菜单，默认显示 **`main`**
3. **点开它，改选 `arena/019fb701-repo`** ← 这一步别漏
4. 再点绿色的 **`Run workflow`** 按钮

> 为什么？因为程序代码都在 `arena/019fb701-repo` 这个分支上，
> `main` 分支是空的，在那里跑会找不到文件。

# 第 3 步 · 等待

出现**黄色转圈** 🟡 = 正在打包。

**去泡杯茶，等 8-10 分钟**，刷新页面看进度。

变**绿色对勾** ✅ = 成功。

# 第 4 步 · 下载安装

1. 点那条**绿勾**记录
2. 滚到页面**最底部**，找到 **`Artifacts`**
3. 点 **`MediaForge-Windows-Setup`** 下载
4. 下载的是 zip，**右键 → 全部解压缩**
5. 双击里面的 **`MediaForge-1.0.0-Setup.exe`**

> ⚠️ 会弹**蓝色警告**"Windows 已保护你的电脑"。
> 不是病毒，只是没买数字签名证书（一年几千元）。
> 点 **`更多信息`** → **`仍要运行`**。

一路"下一步"装完。FFmpeg 已打包在内，**不用再装别的东西**。

---

# 怎么用

双击桌面图标：

1. 把文件**拖进**窗口
2. 右上角选**输出格式**（mp4 / mp3 / jpg …）
3. 图省事就在**预设**里挑一个（28 个现成的）
4. 点左上角 **`开始转换`**

---

# 还是失败？

点进红叉那一步，展开日志，**把报错截图发我**。

以后要新版本：重复第 1~2 步即可（记得选对分支）。
