# REFERENCE TEMPLATE ONLY — not used by GitHub Actions.
# The real workflow lives at: .github/workflows/build-windows.yml
# Do not copy this file into .github/ without reviewing the full tag-driven
# release pipeline (version parse, APP_VERSION, SHA256, softprops/action-gh-release).
#
# Kept here so older docs/tests that mention packaging/ci/build-windows.yml
# still find a pointer. Prefer editing .github/workflows/build-windows.yml.

name: Build & Release Windows Installer (template — DO NOT USE)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: |
          echo "This is a reference template only."
          echo "Use .github/workflows/build-windows.yml instead."
          exit 1
