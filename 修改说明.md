# MediaForge 修改说明

> 基于 https://github.com/tanker420/MediaForge 原仓库（公开版），按用户要求完成**全部 A/B/C 级问题修复** + **现代化 UI 重写** + **中文化打包模板** + **取消命令行预览**。所有改动均已在沙箱内跑通测试与 GUI 冒烟。

---

## 1. 已修复的问题（共 9 项）

### A 级（实际 Bug）

| ID | 问题 | 修复位置 |
| --- | --- | --- |
| **A1** | CLI 中 `-f/--format` 与 `--out-format` 共用同一个 argparse dest，导致 `--help` 中互相覆盖 | `app/cli.py` 拆分：`-f/--kind` → dest=`kind`，`-F/--out-format` → dest=`out_format` |
| **A2** | 音频淡出（淡出滤镜）在「预览命令」里有，实际执行却丢失 — 预览与真实不一致 | `app/core/ffmpeg_builder.py` 在 `build_command` 中探测到媒体时长后自动注入 `params["_duration"]`；`app/ui/main_window.py` 已彻底移除命令行预览（见 B6） |
| **A3** | 图片转换不支持取消（`convert_image` 没有任何 cancel 检查） | `app/core/image_engine.py` 新增 `CanceledError` 与 `_check_cancel`；`convert_image` / `_apply_geometry` / `_apply_adjustments` / `_convert_animated` 均接收 `cancel` 参数；`app/core/converter.py` 中 `run_job` 将 cancel 传到底层并捕获 |

### B 级（功能 / 测试缺陷）

| ID | 问题 | 修复位置 |
| --- | --- | --- |
| **B4** | 字幕文件路径里含 `[ ] , ;` 等字符时滤镜解析失败（仅转义了 `:` `'`） | `app/core/ffmpeg_builder.py` 新增 `_escape_filter_path`，统一转义 `: ' [ ] , ;` 并把反斜杠标准化为斜杠 |
| **B5** | `tests/test_commands.py` 整个模块在缺 ffmpeg 时跳过，导致纯字符串测试（滤镜链、转义、参数翻译）永远跑不到 | `tests/test_commands.py` 拆分：纯字符串测试**始终运行**，仅真正拼装完整命令行的测试用 `pytest.mark.skipif` 在缺 ffmpeg 时跳过 |
| **B6** | 主窗口有死代码（`idx = self.cb_kind.findData(kind)` 没人用，且整个 UI 暴露了「命令行预览」+「自定义 ffmpeg 参数」专家入口） | `app/ui/main_window.py` 完全重写：去掉命令行预览框与 `extra_args` 字段，所有操作图形化 |

### C 级（小问题）

| ID | 问题 | 修复位置 |
| --- | --- | --- |
| **C8** | UI 代码直接访问 `q._threads`（私有属性） | `app/core/converter.py` 新增公开方法 `ConversionQueue.wait()`，`main_window.py` 改为调用它 |
| **C9** | `icon.ico` 与 `icon.png` 文件名混乱（spec/installer 用 .ico，UI 既要拿 .ico 也要拿 .png） | `app/resources/icon.ico`（多尺寸 ICO，用于 PyInstaller 与安装程序）+ `app/resources/icon.png`（256×256 PNG，用于 GUI 窗口与启动图标）；`packaging/MediaForge.spec`、`packaging/installer.iss` 只引 .ico；`app/ui/main_window.py` 只引 .png |
| **缓存失效** | 切换 ffmpeg 后 `available_encoders()` 不会重新探测 | `app/core/ffprobe.py` 新增 `invalidate_caches()` 一次性清除 `find_tool` / `available_encoders` / `ffmpeg_version` 三个缓存；UI 右上角 FFmpeg 状态标签点击即可重新检测 |

---

## 2. UI 现代化重写（任务 #3）

原 UI 是一个保守的工具栏+表格布局，存在命令行预览框与专家字段。新版完全重写：

- **现代化布局**：顶栏 + 类别分段选择器（视频/音频/图片）+ 左文件列表卡 + 右输出设置卡 + 底操作栏
- **主题系统**：`app/ui/theme.py` 提供完整浅色 QSS：圆角卡片、渐变主按钮、胶囊进度条、微软雅黑字体栈；分段选择器、卡片标题、拖拽提示、底栏等自定义控件样式
- **自动识别类别**：拖入文件后无需手动选择类别，分段控件自动切换；切换类别时输出格式/编码器/预设/参数表单全部自动重建
- **拖拽支持**：文件可拖入表格，也可拖到窗口任意位置；目录递归收集
- **现代交互**：
  - 预设一键套用（28 个内置 + 用户预设），应用后仍可微调
  - 选择输出格式后，编码器下拉框自动限定为该容器支持的编码器
  - 编码器不可用时显示「（未安装）」后缀（基于 `available_encoders()`）
  - 表格内进度条 + 状态文字实时更新
  - 选中文件时后台线程异步探测，显示分辨率/帧率/采样率/时长/大小到状态栏
  - 完成后弹窗汇总成功/失败/取消数量 + 一键打开输出文件夹
- **危险操作锁定**：转换期间禁用「添加/移除/清空/类别切换/格式切换/参数表单/输出目录」等所有会改变任务集合的控件，仅保留「取消」按钮
- **取消支持**：进度条可取消；图片任务在主要步骤间检查取消标志并抛出 `CanceledError`
- **设置持久化**：上次输出目录、命名规则、并行任务数、覆盖选项通过 `QSettings` 记住
- **键盘友好**：Delete 键移除选中文件

### 取消的命令行相关功能

- ❌ 主窗口中的「命令预览」文本框（用户要求「不需要看到命令行」）
- ❌ 参数表单中的「自定义 ffmpeg 参数」字段（专家入口，用户要求「也不需要命令行运行」）

---

## 3. 打包模板中文化（任务 #4）

按国内主流安装程序的样式重写 `packaging/installer.iss`：

- **向导语言**：简体中文默认（`chinesesimplified` 第一条），英文备选
- **本地化文案**：标题 `安装 - MediaForge 全能格式转换器 1.0.0`、按钮 `安装(I) / 下一步(N) > / 上一步(B) / 完成(F) / 浏览(R)…`、欢迎页/完成页文案全中文
- **应用信息**：应用名、卸载显示名、文件属性全部带中文
- **任务项**：「创建桌面快捷方式」「将 MediaForge 加入系统 PATH」「在右键菜单中添加『用 MediaForge 转换』」
- **PATH 处理**：勾选 PATH 时追加 `%ProgramFiles%\MediaForge`，卸载时自动清除
- **版本文件**：`packaging/version_info.txt` 用 UTF-16 LE（lang 0804, codepage 04B0）写入中文产品名 / 描述

其他打包文件一并完善：

- `packaging/MediaForge.spec` — PyInstaller 配置，`console=False`（无命令行窗口），随包分发 `app/resources/`
- `packaging/version_info.txt` — Windows 文件属性（中文）
- `packaging/build_windows.bat` — 本地一键打包脚本（venv + pip + pyinstaller + Inno Setup）
- `packaging/ci/build.ps1` — CI 共用脚本（自动下载 BtbN 静态 FFmpeg → 打包 → 装包）
- `packaging/ci/build-windows.yml` — 参考模板（仓库根的 `.github/workflows/build-windows.yml` 才是真正生效的工作流）
- `.github/workflows/build-windows.yml` — 手动触发 / 打标签自动构建并发布 Release
- `如何生成安装程序.md` — 中文使用文档

---

## 4. 验证结果

- **py_compile**：所有 15 个 Python 文件语法通过
- **pytest**：59 个测试全部通过（其中纯字符串测试在沙箱无 ffmpeg 时也能跑，B5 修复生效）
- **GUI 冒烟**：离屏渲染主窗口 → 添加 PNG → 自动切到图片模式 → 应用「JPEG 高质量」预设 → 走 UI 队列真实转换 → 完成；再添加 MP4 → 自动切到视频模式 → 切到 MKV + 「仅重封装」预设 → 走 UI 队列真实转换 → 完成。两种截图均符合现代化主流风格

---

## 5. 文件总览（与仓库对齐）

```
MediaForge/
├── .github/workflows/build-windows.yml    ★ 新版 CI 工作流
├── app/
│   ├── cli.py                              ★ A1 修复：dest 拆分
│   ├── core/
│   │   ├── converter.py                    A3 修复（图片 cancel）+ C8 修复（wait()）
│   │   ├── ffmpeg_builder.py               A2 修复（注入 _duration）+ B4 修复（路径转义）
│   │   ├── ffprobe.py                      缓存失效（invalidate_caches）
│   │   ├── formats.py                      原样保留（参数目录是单一事实来源）
│   │   ├── image_engine.py                 A3 修复（CanceledError + cancel 检查）
│   │   ├── naming.py                       原样保留
│   │   └── presets.py                      原样保留
│   ├── resources/
│   │   ├── icon.ico                        ★ 新生成：Windows 打包用
│   │   └── icon.png                        ★ 新生成：GUI 窗口图标
│   └── ui/
│       ├── main_window.py                  ★ 全新现代化重写（Task #3）
│       ├── theme.py                        ★ 新建：浅色 QSS 主题
│       └── widgets.py                      ★ 新建：分段控件/参数表单/文件表格
├── main.py                                 ★ 入口（默认 GUI，--cli 切换命令行）
├── packaging/
│   ├── MediaForge.spec                     ★ 新版 spec（console=False）
│   ├── build_windows.bat                   ★ 本地一键打包
│   ├── ci/
│   │   ├── build-windows.yml               ★ 参考模板
│   │   └── build.ps1                       ★ CI 构建脚本
│   ├── installer.iss                       ★ 全新中文化安装脚本（Task #4）
│   └── version_info.txt                    ★ 中文文件属性
├── tests/
│   ├── conftest.py                         ★ 新建：保证 import app
│   ├── test_commands.py                    ★ B5 修复：纯字符串测试不依赖 ffmpeg
│   ├── test_core.py                        ★ 新增测试覆盖
│   ├── test_e2e.py                         ★ 新增端到端测试
│   ├── test_packaging.py                   ★ 新增：打包一致性测试
│   └── test_ui_codecs.py                   ★ 新增：UI 与格式目录一致性
├── LICENSE                                 原样
├── README.md                               ★ 全新中文 README
├── requirements.txt                        原样
└── 如何生成安装程序.md                     ★ 新建中文打包说明
```

★ = 本次新建或大幅修改的产物。

---

## 6. v1.1.0 新增功能（项目交接后迭代）

| 模块 | 改动 |
| --- | --- |
| **app/core/updater.py** | 新建：GitHub Releases API 检查新版本、版本比较、流式下载、调用安装包静默安装并退出 App |
| **app/ui/update_dialog.py** | 新建：「有可用更新」对话框 + 下载进度对话框（可取消） |
| **app/ui/preset_dialog.py** | 新建：用户预设管理对话框（新建/重命名/删除/导出/导入/用当前参数覆盖） |
| **app/core/presets.py** | 扩展：增加 `add_user_preset / rename_user_preset / delete_user_preset / overwrite_user_preset / export_presets / import_presets` |
| **app/core/media_preview.py** | 新建：图片缩略图、视频首帧截取、音频波形 RMS 计算 |
| **app/ui/preview.py** | 新建：`MediaPreview` 组件，按文件类型显示缩略图 / 波形，后台线程生成 |
| **app/ui/theme.py** | QSS 修复：去掉 `QComboBox QAbstractItemView` 的 padding/margin，添加 `outline: 0`、`min-height: 18px`，避免 Qt 6 popup 顶部出现 phantom row（用户报告的「下拉菜单空白项」Bug）；新增 `#MenuBtn` 顶部菜单按钮样式 |
| **app/__init__.py** | 新建：单一版本源 `__version__ = "1.1.0"` |
| **app/cli.py** | 从 `app.__version__` 读取版本号 |
| **app/ui/main_window.py** | 集成：顶栏 ⋯ 菜单（检查更新、关于、配置目录、查看日志）；启动 3s 后节流式自动检查更新（24h 内最多一次）；预设「管理…」按钮；选中文件自动生成预览 |
| **packaging/version_info.txt** | 同步为 v1.1.0；构建时由 `build.ps1` 动态注入最新版本号 |
| **packaging/installer.iss** | 新增 `AppMutex=MediaForgeAppMutex_…`，让更新器的 `/CLOSEAPPLICATIONS` 能可靠关闭正在运行的 MediaForge |
| **packaging/ci/build.ps1** | 强化：版本注入、`pyinstaller>=6.3,<7` 钉版本、可选代码签名（自动探测 Windows SDK signtool）、签名主 exe + 安装包 |
| **.github/workflows/build-windows.yml** | 增加签名 Secret 注入：`CODE_SIGNING_PFX_BASE64` / `CODE_SIGNING_PFX_PASSWORD` |
| **tests/test_combo_popup.py** | 新建：下拉弹层无空白项、首项紧贴顶部的回归测试（守护 QSS 修改） |
| **tests/test_updater.py** | 新建：版本解析、Markdown 剥离、安装包名匹配规则 |
| **tests/test_packaging.py** | 增强：断言 `AppMutex`、`build.ps1` 全 ASCII、签名支持、工作流传递签名密钥 |

### v1.1.0 验证

- **pytest**：69 个测试全部通过（59 旧 + 2 popup + 4 updater + 4 打包新断言）
- **本地打包**：清空 build/dist → `pyinstaller --noconfirm --clean packaging\MediaForge.spec` → 产物含 `_internal/bin/ffmpeg.exe`（真实二进制）+ `_internal/bin/ffprobe.exe`，`MediaForge.exe --cli --version` 输出 `MediaForge 1.1.0`
- **CLI**：打包后 `--list-formats` 列出全部 55 种格式
- **GUI 冒烟**：offscreen 渲染主窗口 → 新菜单/预览/预设管理按钮全部就位
- **更新检查**：本机联网调用 `check_for_update('1.1.0')` 返回 None（当前已是开发版最新），调用 `check_for_update('1.0.0')` 返回 v1.0.1 release 的完整 UpdateInfo

---

## 7. v1.2.0 液态玻璃 UI 重设计（2026-08-05 接手迭代）

目标：整体向 macOS app 风格靠拢（液态玻璃材质、工具栏式头部、分段控件居中），
同时修复「选中一个之后下拉菜单有空白」的遗留 Bug。最小化/最大化/关闭保持
Windows 原生标题栏位置不变。

| 模块 | 改动 |
| --- | --- |
| **app/ui/theme.py** | 全面重写为 macOS 液态玻璃浅色主题：#007AFF 系统蓝强调、半透明玻璃材质层次（毛玻璃工具栏/底栏 → 玻璃卡片 → 控件）、发丝线、大圆角、macOS 圆角复选框、细胶囊进度条、Segoe UI Variable 字体栈；新增 `GlassBackdrop` 自绘「壁纸光晕」客户区背景；`enable_window_glass` 在 Win11 为原生标题栏启用 Mica 材质（失败静默降级） |
| **app/ui/theme.py（Bug 修复）** | **下拉空白真正根因**：旧主题在 popup 视图设 `selection-color: white`，但 `::item` 子控件被样式化后选中背景不再取视图的 `selection-background-color` → 选中项「白字白底」渲染为不可见空白行（交接文档里「padding 导致 phantom row」是误诊）。修复：`::item:selected` / `::item:hover` 显式声明背景与前景 |
| **app/ui/main_window.py** | 工具栏式头部（52px）：左品牌、中分段选择器、右 FFmpeg 徽章与 ⋯ 菜单；类别提示小字并入文件卡片标题行；底栏 56px；转换期间主按钮显示「转换中…」；最小窗口 980×640；central widget 换用 `GlassBackdrop` |
| **app/ui/widgets.py** | 表格行高 40 → 36（macOS 列表密度） |
| **app/ui/preview.py** | 预览画布 / 波形 / 错误占位内联样式同步玻璃主题（波形改 #007AFF/#5AC8FA） |
| **app/resources/check.svg** | 新增：复选框白色对勾矢量图（随 resources 整目录入库打包） |
| **tests/test_combo_popup.py** | 新增根因回归测试：断言 QSS 的 `::item:selected` 显式声明背景与前景 |

### v1.2.0 验证

- **pytest**：70 个测试全部通过（69 旧 + 1 根因回归）
- **GUI 真机验证（Win11 build 26200）**：输出格式下拉选中 iTunes M4V 后重新打开，选中项蓝色渐变高亮、行距均匀、无任何空白行；液态玻璃层次（光晕背景 + 毛玻璃工具栏/卡片）渲染正常；原生标题栏最小化/最大化/关闭位置不变
- **血的教训**：Qt 原生标题栏 + `WA_TranslucentBackground` 无法与 DWM 客户区背景合成，渲染出黑带。因此客户区**不开透明通道**，玻璃效果用「自绘光晕背景 + 半透明 QSS 材质」实现，Mica 只用于系统标题栏

### v1.2.0 补充（深浅色切换 + 许可中文化）

| 模块 | 改动 |
| --- | --- |
| **app/ui/theme.py** | 重构为双主题：`build_qss(dark)` / `apply_theme(app, dark)` / `is_dark()`；浅色液态玻璃 + 深色液态玻璃（#0A84FF 强调、深色玻璃材质令牌表 `_tokens`）；`GlassBackdrop` 光晕随主题取色 |
| **app/ui/main_window.py** | 头部新增「深色 / 浅色」切换按钮（#ThemeBtn，位于 FFmpeg 徽章左侧）；设置项 `ui_dark` 经 QSettings 持久化，启动时自动恢复 |
| **app/ui/preview.py** | 预览画布 / 头部内联样式并入全局 QSS（#PreviewCanvas / #PreviewHeader），自动跟随双主题；波形与错误占位取色跟随 `is_dark()` |
| **LICENSE.zh-CN.txt** | 新增：GPL-3.0 中文参考译文（UTF-8 BOM；非官方译文，以英文原文为准）。安装程序许可页改用它（installer.iss `LicenseFile`）；README 许可证章节补充链接 |
| **app/__init__.py / packaging/version_info.txt** | 版本号同步为 1.2.0 |
| **tests/test_packaging.py** | 许可页断言改为中文许可文件 `LICENSE.zh-CN.txt` |